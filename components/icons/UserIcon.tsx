// This component is no longer in use.
// The UI has been refactored to simulate a live phone call from a dashboard
// rather than displaying a persistent chat history with user icons.
import React from 'react';

const UserIcon: React.FC = () => null;

export default UserIcon;